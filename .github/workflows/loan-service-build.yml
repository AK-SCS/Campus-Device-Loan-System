name: Loan Service - Build

on:
  push:
    branches: [main]
    paths:
      - 'loan-service/**'
      - '.github/workflows/loan-service-build.yml'
  pull_request:
    branches: [main]
    paths:
      - 'loan-service/**'
      - '.github/workflows/loan-service-build.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./loan-service

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: loan-service/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test

      - name: Build (into ./dist)
        run: npm run build

name: Device Catalogue - Build and Deploy to Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci
        working-directory: ./device-catalogue-service

      - name: Build (into ./dist)
        run: npm run build
        working-directory: ./device-catalogue-service

      - name: Publish function app to package folder
        run: |
          # make an empty package folder
          rm -rf package
          mkdir package

          # copy root metadata
          cp host.json package/
          cp package.json package/
          cp package-lock.json package/

          # copy compiled JS
          cp -r dist package/

          # install runtime dependencies only (no devDeps) into the package
          cd package && npm ci --omit=dev
        working-directory: ./device-catalogue-service

      - name: Save package temporarily for deployment
        uses: actions/upload-artifact@v4
        with:
          name: functionapp-package
          path: device-catalogue-service/package
          retention-days: 1

  deploy-to-test:
    needs: build-and-publish
    runs-on: ubuntu-latest

    steps:
      - name: Download package artefact
        uses: actions/download-artifact@v4
        with:
          name: functionapp-package
          path: package

      - name: Deploy to Azure Function App
        uses: azure/functions-action@v1
        with:
          app-name: 'campus-device-catalogue'
          package: package
          publish-profile: ${{ secrets.AZURE_DEVICE_CATALOGUE_PUBLISH_PROFILE }}

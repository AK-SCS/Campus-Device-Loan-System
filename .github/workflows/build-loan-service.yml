name: Build - Loan Service

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'loan-service/**'
      - '.github/workflows/build-loan-service.yml'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'loan-service/**'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: loan-service/package-lock.json
        
    - name: Install dependencies
      run: npm ci
      working-directory: ./loan-service
      
    - name: Run unit tests
      run: npm test
      working-directory: ./loan-service
      
    - name: Generate coverage report
      run: npm run test:coverage
      working-directory: ./loan-service
      
    - name: Build
      run: npm run build
      working-directory: ./loan-service
      
    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      with:
        name: loan-service-build
        path: loan-service/dist
        retention-days: 1
